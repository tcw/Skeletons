This project contains simple maven skeleton projects.
The projects are as simple as possible.

The following skeletons can be found under /guice:

* jerseyGuice    -Jersey with Guice-Servlet and Guice (JSR-330)
* simpleGuice    -Guice-Servlet and Guice (JSR-330)
* filteringGuice -Guice-Servlet and Guice with filtering (JSR-330)

To try them out:

mvn jetty:run
http://localhost:8080/hello


